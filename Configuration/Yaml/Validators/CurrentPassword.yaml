prototypes:
  standard:
    validatorsDefinition:
      CurrentPassword:
        implementationClassName: WapplerSystems\FeRegistration\Validator\CurrentPasswordValidator
        formEditor:
          iconIdentifier: form-validator
          label: 'Current FE-User password matches'